# External registration blocker

External site registration through the connected Opera browser currently cannot proceed because the Browser Connector reports that `Allow AI connection` is not enabled/signed in inside Opera.

GitHub-connected and email-submittable work continues independently.
