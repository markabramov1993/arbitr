name: Base DEX Arb Scan

on:
  push:
    branches: [profit-engine-v07]
    paths:
      - 'scripts/base_dex_arb_scan.py'
      - 'scripts/base_dex_spread_quick.py'
      - 'scripts/cbbtc_focused_quote.py'
      - 'test/CbBtcArbFork.t.sol'
      - '.github/workflows/base-dex-arb-scan.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cbBTC-fork:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable
      - name: Build
        run: forge build
      - name: Execute real Uniswap to Aerodrome swaps on Base fork
        shell: bash
        run: forge test --match-contract CbBtcArbForkTest -vv | tee cbbtc-fork.txt
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cbbtc-fork-evidence
          path: cbbtc-fork.txt
          retention-days: 30

# FLASH manual live/fork refresh: 2026-09-04T20:43Z
