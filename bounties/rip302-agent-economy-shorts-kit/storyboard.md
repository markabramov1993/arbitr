# Storyboard — 9:16 Vertical, 55–60s

All visuals can be captured from public source code or generated as simple vector/terminal graphics. No external footage is required.

| Time | Visual | Motion / edit | On-screen text |
|---|---|---|---|
| 0:00–0:05 | Two terminal windows labelled `Agent A` and `Agent B`, empty space between them | A job card snaps into the center, then a small RTC token icon moves toward it | `Can software hire software?` |
| 0:05–0:13 | Public `rip302_agent_economy.py` header in editor | Slow punch-in on `Agent-to-Agent RTC Economy` | `RIP-302` |
| 0:13–0:23 | Minimal flow diagram: Poster → Job → Escrow | Reward counter and `+5%` counter merge into escrow box | `reward + 5% fee → ESCROW` |
| 0:23–0:34 | Four status cards stacked vertically | Highlight in sequence: open, claimed, delivered, completed; briefly flash disputed/expired as side branches | `OPEN → CLAIMED → DELIVERED → COMPLETED` |
| 0:34–0:44 | Escrow box splits into two arrows | Main arrow to Worker; small arrow to platform wallet | `worker gets reward` / `fee routed separately` |
| 0:44–0:53 | Code/table visual showing reputation fields | Counters increment beside jobs completed, disputes, RTC earned | `reputation = history` |
| 0:53–0:59 | Return to Agent A ↔ Agent B diagram | Connect them with a clean glowing line; end on repository text | `software → escrow → software` |

## Capture instructions

1. Open the public RustChain `rip302_agent_economy.py` source and capture only the relevant header/constants/status sections.
2. Use a monospace font at large size; crop all browser chrome where possible.
3. Generate diagrams from simple shapes/text; do not use logos or media whose rights are unclear.
4. Keep every text card on screen long enough to read on a phone.
5. Use hard cuts or 2–4 frame digital wipes; no flashy transition should obscure code or numbers.
6. Keep the key factual number `5%` visible only while narration explains the platform fee.

## Audio direction

Neutral technical narration, ~140 words/minute. Subtle terminal/UI sounds are optional. No music is required.

## Accessibility

- Burn in subtitles for all narration.
- Minimum 48 px equivalent text in a 1080×1920 canvas.
- Do not rely on color alone for state changes; use labels and position.